{
  "name": "@clapper/cli",
  "version": "0.1.0",
  "description": "Render @clapper compositions to MP4 in headless Chromium, and preview them in a studio.",
  "type": "module",
  "bin": { "clapper": "./bin/clapper.mjs" },
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@clapper/core": "workspace:*",
    "@vitejs/plugin-react": "^5.0.0",
    "ffmpeg-static": "^5.3.0",
    "playwright": "1.62.1",
    "tsx": "^4.20.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^4.0.0"
  }
}
