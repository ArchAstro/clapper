# Independent sampled-content comparison

1. Scope and method
   - Read only each topic's brief.json, source.txt, A/B-scenes.json and A/B.mp4. Source fit means fit to the supplied curator notes, not independently verified publication text.
   - Extracted and inspected every scene midpoint (47 images total), 24 additional journey/transition/detail frames, and two full-size completed queue computations. Sheets and strips remain beside this report. Timestamps below are seconds from the beginning of the corresponding video.
   - Audio unassessed: no listening occurred. This is one focused diagnostic pass, not a full-motion playback, learning-outcome claim, or final approval. First-use definitions in narration and narration/image synchronization are unassessed.

2. Queue: prefer B, moderate confidence, for technical teaching content
   - Causal trace: B tracks request 11 from offer at tick 4 through admission at tick 5 and completion at tick 7 (116s annotation; 239.5s timeline). This connects upstream waiting to measured latency: 7 - 4 = 3 ticks. A's 225s metrics frame defines latency accurately but uses an unnumbered Offer A → Complete A line, making less use of its worked trace.
   - Mathematical completeness: B at 152s explicitly counts old waiting plus new offers: b = 1 waiting + 3 new = 4, then admits min(4,2)=2 and retains 2. It provides q' = q - s + a and w' = b - a. A at 135s correctly computes the first blocked tick, but has no prior waiting in that local example. B therefore more directly exposes how backlog persists into the next tick.
   - Conservation: B 165s shows 12 = 6 + 4 + 2 + 0 and lists the actual item IDs in completed, queued and waiting partitions; separately 10 admitted = 6 completed + 4 queued. A 149s correctly shows 13 = 8 + 4 + 1 and separates admission, but omits those ID lists. Both avoid double-counting cumulative admission.
   - Source fit and operations: both keep capacity 4, overload offers 3/tick and service 2/tick, service before admission, no disappearance of excess, distinct policy names and proper throughput/latency units. B 187.5s compares capacities 4 and 8 with identical 10 completions at tick 6, directly showing unchanged service. A 173s similarly shows delayed saturation. Both visibly label the trace as illustration.
   - Whole before parts: both complete the introductory job before the next scene. A shows completed A at 32–40.8s before its baseline at 41.2s. B reaches completion at 34–38.8s before rules at 39.2s.
   - Strongest evidence against B: the moving-flow views are visually less reliable. At B 91s/116s, the consumer and right portion of its label are cut off by the right frame edge; at 116s jobs overlap the consumer's rate text. The gate is still an X while the caption describes service freeing capacity, a potentially confusing transitional state. In B's first journey at 24s the caption already says admitted while the item is still at the producer; at 29s it says consumer makes thumbnail while the item remains in the queue. A also has some label/position lag at 25s, but its four-stage diagram stays wholly in frame and clearly distinguishes a complete counter. A's 52s → 75s → 99.5s main trace also continues from the baseline when the rate changes, whereas B explicitly resets after its rule example. These favor A for visual causal continuity and polish. B's preference here is narrowly about the more explicit worked backlog/latency accounting, not blanket superiority.

3. Universal Transformers: prefer A, moderate confidence, for complete mechanism explanation
   - Architecture and causal trace: A 52.65s shows embeddings → shared-depth encoder → final states, separate shared decoder parameters, prefix embeddings, projection, softmax, and select/append return. A 76.8s explicitly separates the unchanged six token positions of the depth loop from the growing output prefix. B 63s/89.5s/282.5s also covers this correctly; architecture coverage alone is close.
   - Refinement operations: A 99.88s exposes both residual paths in the complete encoder step, then 144.98s spells out x = h + p + d and a = LayerNorm(x + Attention(x)), with both inputs visibly entering the plus node. At 167.67s it diagrams affine → ReLU → affine and the second skip path, giving h' = LayerNorm(a + F(a)); it also names separable convolution as the alternative in the supplied source. B at 122/127/131s includes the chosen affine → ReLU → affine transition and position/depth signals, but represents residual addition and normalization as small badges above the linear chain. The actual bypass/addition mechanism is not visible in the inspected frames. A turns a source requirement into a traceable operation.
   - Running-example motivation: A 10.55s uses river-bank ambiguity to motivate attention; at 123.38/131s, the same bank query visibly weights river at 0.65 and forms the value mixture. This connects the token-context problem with the later operation more concretely than B's generic cat-sleeps translation. Both appropriately label states as illustrative; neither supplies a measured model trace.
   - Training: A 217.45s separates probabilities, loss against correct rive, backpropagation, optimizer update and repeated examples. At 249/253s it visibly sums g1 + g2 + g3 for one shared parameter. B 176s supplies correct-prefix/next-target alignment and 229.5s provides both summed contributions and the parameter-update equation. Both distinguish fixed-depth training from optional per-position ACT (A 263.27s, B 256s), and both identify encoder-only diagrams. No substitution with later looped-transformer work is visible.
   - Whole before parts: A displays the complete translated sentence at 38/39.3s before opening architecture at 39.6s. B displays the complete sentence and end token at 46/47.8s before architecture at 48.2s. Both satisfy this sampled ordering.
   - Strongest evidence against A: B is better for selected concrete training/math details. At 176s it aligns each correct next token beneath its decoder input, while A's training diagram focuses on a single target. B 203s writes the explicit weighted-value sum 0.15v1 + 0.25v2 + 0.60v3, whereas A's attention view shows weights, multiplications and convergence arrows without a scalar/vector worked result. B 147s shows a triangular causal-mask illustration and defines query/key/value in plain language; A's 192.17s decoder instead relies more on route labels and a schematic qualification. B 229.5s includes the learning-rate update. These are meaningful advantages for an engineer new to attention. A wins on visible residual/normalization mechanisms and coherent motivation, not on every mathematical example.

4. Limits
   - Sampling cannot establish every transition's quality or all claims made in narration. All preferences are diagnostic content judgments. No learning gain or final approval is asserted.
