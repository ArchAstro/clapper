# Independent visible-content review

1. **Preference: B, moderate confidence.** B more clearly connects the completed request journey, sustainable baseline, changed arrival rate, accumulating work, and capacity feedback in one trace. A remains technically substantial and is better on backlog arithmetic and a concrete latency calculation. This is a diagnostic judgment of visible explanatory content, not evidence of actual learning.

2. **Observed scope.** Read only the supplied brief, reference notes, and A/B scene manifests; inspected decoded MP4 frames. Used 10-second overview contact sheets spanning A's 276 seconds and B's 264 seconds, 2-second consecutive-sample strips covering A 18–40 and 103–129 seconds, B 18–42 and 87–113 seconds, and native-size frames A 150/242 and B 134/230 seconds. Sample-strip positions are approximate within their 2-second sampling windows. Did not listen to audio, execute authored source, inspect author logs, consult other reviews, or measure learning. Spoken narration quality/presence and audiovisual synchronization are unassessed.

3. **Whole request before components: both satisfy visually; B is cleaner.** At A 18–39 seconds and B 18–41 seconds, a single marked request moves producer → queue → consumer → completion before the detailed tick rules. B gives completion its own location and a count that changes from 0 to 1. A's token crosses the consumer's large service-rate text around 30–34 seconds and its green “thumbnail complete” badge is already visible before the request completes. The captions rescue the meaning, but B separates capacity, service, and completed output more clearly.

4. **Cause before consequence and trace continuity: B advantage.** B 41–63 establishes two offers and two services per tick, then 63–87 explicitly continues from tick 2 while changing offers from 2 to 3. The visible end of tick 5 has 4 queued, 1 waiting, 8 complete. B 87–112 continues at tick 6, showing the two oldest jobs leaving, completion increasing 8→10, two free slots, the producer's retained job plus new arrivals, two admissions, then 4 queued/2 waiting. Only after that does 112–138 open the tick arithmetic. A instead opens a saturated tick-3 model at 39–62 before establishing its 62–79 baseline; its subsequent overload trace restarts from empty (explicitly labeled). A's reset is honest, but following the causal experiment requires more reconstruction.

5. **Control mechanism: B advantage, with an A strength.** B's 87–112 strip exposes service → free slots → admission in place, labels jobs in service and in transit, and changes feedback from 0 to 2 to 0 free slots. The final text makes the control action explicit: free slots become permission to send and unsent jobs stay upstream. A 103–129 has a legible admission gate and upstream feedback, plus a useful persistent request-11 journey (offered tick 4, admitted tick 5, completed tick 7). However, the control section holds the full tick-4 state for roughly its first half, then compresses several subsequent ticks; its tokens overlap in the producer and consumer during transitions. B makes the causal transition easier to inspect.

6. **Conservation and units: both largely faithful; A has useful extra rigor.** A 154–176 shows 12 = 6 completed + 4 queued + 2 waiting + 0 dropped, with 10 admitted = 6 completed + 4 queued. B 138–160 shows 13 = 8 + 4 + 1, explicitly labels the settled end of tick 5/no jobs in transit, states zero drops, and brackets 12 admitted = 8 completed + 4 queued. These different totals follow their explicitly different traces and are not a contradiction. A's 150-second frame includes candidates = 1 waiting + 3 new = 4 and carries the remainder. B's 134-second frame correctly replays the first saturated tick, min(3,2)=2, but does not algebraically generalize to an existing backlog. A 242 seconds concretely computes request-11 latency 7−4=3 ticks, including upstream wait. B 230 seconds correctly distinguishes completed jobs/tick from time for one job, but uses an unnumbered latency arrow rather than a measured example. A also explicitly defines a tick as a model time step at 39–62; that definition was not visible in B's inspected frames.

7. **Buffer and policy fidelity: both pass the inspected content.** A 176–199 and B 160–186 compare finite capacities under unchanged arrival/service rates and retain excess upstream rather than silently losing it. B explicitly shows saturation ticks 2 versus 6 and later upstream growth. Both say larger capacity delays saturation and does not fix sustained overload. A 199–227 and B 186–213 distinguish waiting, blocking, dropping, retrying and demand signaling; neither visibly suggests backpressure creates service capacity. Both label the visuals as illustrations.

8. **Remaining defects and limits.** B should define tick on first use, show one measured latency, and add an existing-backlog arithmetic example so the retention rule cannot be mistaken for merely new offers minus admissions. In B's journey around 24–26 seconds and control around 93–101 seconds, occupancy/“in service”/“in transit” labels update at phase boundaries while moving tokens may still overlap old locations; this is mostly explained by the labels, but deserves tighter synchronization. A should remove the prematurely present completion badge, give service tokens clear space, and avoid introducing a saturated local model before the baseline experiment. Neither inspected buffer comparison actually switches a burst back down to sustainable load; both visibly demonstrate sustained overload better than transient-burst recovery. Audio remains unassessed, so this preference is not a full acceptance judgment against the narration requirement.
