# Independent Universal Transformers visible-content review

1. **Preference: B, moderate confidence.** Both visibly fit the supplied original Universal Transformers architecture. B makes the refinement step substantially more operational: the attention mixture, both residual paths, normalization, and the position-wise transition become connected diagrams rather than mostly labeled boxes. A is stronger on its first full translation walkthrough, explicit attention equation, and shifted training targets. This is a content judgment, not a measured learning result.

2. **Evidence and limits.** Read only this packet's brief, reference notes and scene manifests. Inspected actual decoded frames: 10-second overview samples across A (296 seconds) and B (about 297.1 seconds); consecutive 2-second sampled strips starting at A 23 and 101 seconds, B 66 and 88 seconds; native frames A 205, B 131/150/174/195/248. Each tiled strip contains 18 samples over approximately 36 seconds, including the following scene where applicable: the extraction's output-duration option did not shorten the tile's input span. Filenames retain the originally requested shorter interval, so use these actual spans when interpreting them. No audio listening, authored-code execution, other reviewer consultation, or assessment of actual learning. This is source fit to the supplied notes, not an independent audit of the full paper.

3. **Original architecture: both pass the inspected core.** A 48–78 and B 39.5–65.8 show source embeddings → encoder → final source states, then a separate decoder consuming available output-prefix embeddings, reading encoder states through cross-attention, and producing vocabulary probabilities. Both place selection/appending in the outer generation loop and state that encoder and decoder have separate parameter sets reused across depth. A 78–101 and B 65.8–87.8 distinguish revision of the same positions from sequence growth; both label encoder-only detail views. B's 66–102 strip particularly clearly holds all six source positions fixed while revision changes, then grows the output prefix. Neither visibly substitutes a later looped-model architecture.

4. **Whole before parts: both achieve it, A has the clearer first whole journey.** A 23–48 explicitly animates read/refine → predict → append, grows “Le chat dort” through an end token, and only then opens the two-network architecture. B 21.1–39.5 first completes “Le bateau a atteint la rive,” but initially hides the mechanism inside one “Build context → generate tokens” box; its next architecture scene supplies the full route before any refinement internals. Both complete a refinement step before opening attention. B also places its full training loop at 204.2–230.7 before fixed-depth gradient detail. A places its training overview at 162–190 before its attention detail at 190–216; this slightly separates the attention explanation from the step it explains.

5. **Worked refinement: decisive B advantage.** At B 87.8–112 the complete encoder-step diagram has explicit position/depth inputs, attention, an add-and-normalize operation, a transition, a second add-and-normalize operation, and next-state output. The sampled sequence follows those operations in order. B 134.8–155.2 then exposes both operands of the first residual: x=h+p+d, the bypass x, Attention(x), their addition and LayerNorm. B 155.2–180.1 follows a through affine → ReLU → affine, merges a+F(a), and normalizes; the 174-second native frame labels multiplication plus bias, max(0,feature), and the separable-convolution alternative. A 101–132 shows a clean broad route and later adds “residual addition + normalization” badges and affine→ReLU→affine text, but does not draw the bypass operands. B better shows what those operations actually do and where they enter.

6. **Attention and running example: B better motivated; A mathematically more explicit.** B's opening river-bank ambiguity motivates context and its 112–134.8 attention view returns to that exact word, with six value vectors weighted 0.03/0.12/0.05/0.05/0.65/0.10 into one mixture. The weights sum to one, and thicker river contribution makes the illustrated lookup visually intelligible. A 190–216 uses the same cat sentence as its broader example, and at 205 explicitly shows the weighted sum and scaled query-key softmax formula, defining K and key length. A also warns that attention weights are not a transcript of reasoning. B's query×key→scores→softmax chain is friendlier but does not calculate its scores or show how query/key/value vectors were obtained. Both label states/numbers as illustrations rather than measured traces.

7. **Decoder, training and adaptive depth: both broadly faithful.** A 132–162 has an explicit triangular masking illustration and separates self-attention from source cross-attention. B 180.1–204.2 labels current/earlier prefix access, source keys/values and decoder queries, then gives an illustrative next-token distribution (rive .75, banque .15, other .10). A 162–190 visibly aligns shifted correct-prefix tokens with targets; B 204.2–230.7 shows the entire predictions→loss→backpropagation→optimizer-update loop and states training uses correct prefixes. A 216–243 and B 230.7–253.7 explain accumulation from repeated uses of shared parameters under fixed depth. Both then label ACT optional and per-position, and explicitly distinguish it from the fixed-depth gradient explanation. B 253.7–272.8 also says the bank illustration does not imply a universal five-step rule.

8. **Remaining defects and uncertainties.** B should define query/key/value and softmax before relying on them, and compute one small numerical value mixture rather than stopping at weighted arrows. The B 248-second gradient diagram's g1/g2/g3 branches hang from a common backward line without visibly connecting to the corresponding step uses; a beginner can see a sum but not trace the chain-rule contributions. B's initial “weighted conversation” metaphor should be bounded as clearly as A's warning against interpreting weights as reasoning. A needs actual residual wiring and a connected transition diagram; its current labels do not fully teach those operations. Both use simplified vector pictures and abstract loss/gradient labels, so neither demonstrates a complete numeric training update or adaptive-halting derivation. Audio, narration definitions and audiovisual synchronization remain unassessed; absence of visible definitions does not establish absence in narration.
