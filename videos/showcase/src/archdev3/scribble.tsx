/** The Scribble rig now lives in the framework; this module keeps the film's local names. */
export { Scribble, SCRIBBLE_POSES as POSES, useScribblePose as usePose, SCRIBBLE_INK, SCRIBBLE_PAPER, SCRIBBLE_GREY } from "@agenticvids/core/rigs";
export type { ScribblePose as SPose, ScribblePoseKey as SKey, ScribblePoseName as PoseName } from "@agenticvids/core/rigs";
