/** The monoline developer now lives in the framework; this module keeps the film's local names. */
export { Person, Archie, Desk, PERSON_POSES as POSES, usePersonPose as usePose } from "@agenticvids/core/rigs";
export type { PersonPose as Pose, PersonPoseKey as PoseKey, PersonPoseName as PoseName, ArchieState } from "@agenticvids/core/rigs";
